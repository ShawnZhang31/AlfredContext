# 摩擦日志（Friction Log）

> **这是什么**：手工自举 AlfredContext 时，每遇到一处「协议用起来别扭」就记一笔。
>
> **为什么重要**：这份日志**就是需求清单**，比任何需求文档都真实。自举自己设计的 schema 容易产生确认偏误——你会无意识地把行为迁就 schema。强制记录"我在这里犹豫了"，是对冲手段。
>
> 每条摩擦有三种归宿：① 修正协议；② 记为待办（`Q-*` 事件）；③ 确认是设计取舍，明确不必改。

---

## F-001 · 人工录入没有 transcript 可引用 🔴 已修正协议

**现象**：协议 §7.3 铁律 1 规定「任何 `actor: user` 的事件，其 `evidence.lines` 必须指向 transcript 中用户的原始语句，定位不到则强制降级为 `agent_hypothesis`」。

但手工自举时**根本没有 transcript**——用户的话在我（Agent）的上下文里，不在任何文件里。按字面执行，我录的每一条 `REQ`/`COR` 都得降级成 `agent_hypothesis`，那这批手工账本就废了。

**根因**：铁律 1 的**目的**是「防止提炼器凭空制造最高权威约束」。这个风险只在**从 transcript 推断归属**时存在。人工录入时，作者身份**由构造保证**——是用户自己敲的，无需引用。

**修正**：铁律 1 限定适用范围为 `origin: extracted`。人工录入（`origin: manual`）以 `evidence.note` 记录来源说明即可。

**产出**：`DEC-013`。

**这是协议首个由自举发现并修正的缺陷。** 若不实际用一遍，这个洞会一直留到 `alfredc record` 实现时才暴露——那时改的是代码 + 迁移数据。

---

## F-002 · `constraints.md` 缺少 Superseded 段落 🟡 已修正协议

**现象**：协议 §6.2 只规定 `constraints.md` 编译 `status: active` 的事件。但我在手工编译时发现无从回答审计问题——「它当初为什么被否掉？」

按 §7.1 的 `supersedes` 链，被废止的事件**必须留痕**才有审计价值。若 `constraints.md` 只列 Active，读者（或下一个 Agent）只看到"不许用 X"，看不到"曾经用过 X，因为 Y 被否"。

**修正**：`constraints.md` 增加 **Superseded 段**，列出被废止事件及其继任者，标注删除线。这些**不参与注入**（§9.4 边界：注入只含 Active），仅供审计查阅。

**产出**：协议 §6.2 补充说明。

---

## F-003 · 「全局作用域」没有表示法 🟡 已修正协议

**现象**：`scope` 字段是路径 glob 数组，用于 `--task` 过滤与注入匹配。但项目级决策（如「用 Go」）作用于整个仓库，`scope` 该填什么？协议没定义。

我暂填了 `["**"]`，但这是我自己发明的，不是协议规定的。

**修正**：明确约定 `**` 表示项目全局。同时约定：**`scope` 为空数组视为全局**，两种写法等价，`alfredc init` 统一规范化为 `["**"]`。

**产出**：协议 §5.1 补充说明。

---

## F-004 · 人工生成 `key` 很别扭 🟢 设计取舍，不必改

**现象**：`key = sha1(session|turn|type|summary)`，手工录入时我得凭空编 8 位十六进制。实际上我编的**不是真实哈希**，是随手写的假值。

**判断**：**这不是缺陷。** `key` 是**机器幂等键**，其唯一消费者是提取器（§9.7），人工从不需要计算它。手工阶段填占位值、由 `alfredc record` 在正式录入时补算真实值即可。

**保留观察**：若将来发现人工录入的 `key` 与自动提取的 `key` 会冲突（例如同一条事实被两种途径各记一次），再引入「人工录入的 key 前缀加 `manual:`」的规则。

---

## F-005 · `CLAUDE.md` 用符号链接指向 `AGENTS.md` 🔴 需在 `alfredc init` 中处理

**现象**：本仓库现有的 `CLAUDE.md` 是一个指向 `AGENTS.md` 的**符号链接**（`CLAUDE.md -> AGENTS.md`），而非协议 §10 建议的"`CLAUDE.md` 内含 `@AGENTS.md` 一行"。

符号链接方案在 macOS / Linux 上更彻底（**真正单文件**，不会出现两份内容不同步），是个聪明的做法。

**但它跨平台有隐患**：Windows 上创建符号链接需要开发者模式或管理员权限；若从 git 检出而没有权限，`CLAUDE.md` 会退化成**一个内容为路径字符串的普通文本文件**，Claude Code 读到它就完全失效。而且这种故障是静默的。

**结论**：
- 本仓库继续用符号链接（作者是 macOS 单机开发，且它确实更优雅）。
- 但 **`alfredc init` 必须生成含 `@AGENTS.md` 的真实文件**，而非符号链接——工具要为最广的用户环境负责。
- 这条差异要写进 `alfredc init` 的实现说明。

**产出**：记录于 `.alfredc/current.md` 的 Open Questions。

---

## F-006 · 「派生视图可随时重建」在无 CLI 时是空话 🟢 设计取舍，符合预期

**现象**：协议 §3.2 铁律说派生视图「由 `alfredc` 从账本编译生成，可随时重建」。但 CLI 还不存在，我是**手工编译**的——`constraints.md`、`current.md`、`requirements.md` 三份都是我逐条对着账本抄的。

**判断**：**这正是自举的意义所在。** 手工编译暴露了编译器必须处理的每一个细节（见 F-002、F-003）。等 `alfredc context` 实现时，这些规则已经被验证过了。

**风险提示**：手工维护的派生视图**会与账本漂移**（我已手工同步一次，下次对话若继续追加事件就会不同步）。因此 `current.md` 已注明手工阶段的这一限制，`AGENTS.md` 也加了提醒。

---

## F-007 · §6.2 的示例没有覆盖全部权威分组 🟡 已修正协议

**现象**：协议 §7.3 的表格规定 `project_fact` **应进** `constraints.md`，但 §6.2 的示例只演示了 `## Critical` 和 `## High` 两个分组，没有 `Project Facts` 分组。我照着示例格式手工编译时，**漏掉了 `FACT-001`**——直到跑交叉核对脚本才发现。

**根因**：示例即规范。**读者会照着示例做，而不是照着规则表做。** 示例不完整 = 规则不完整。

**修正**：§6.2 示例补全全部权威分组（Critical / High / Accepted Decisions / Project Facts / Superseded），并明确**分组顺序即权威降序**。

**产出**：协议 §6.2 补充说明。

> 附带收获：这个疏漏是**交叉核对脚本**抓到的，不是人眼。这正好印证了协议 §12.3 第 1 层测试（账本不变量）的价值——**派生视图与账本的一致性应当被自动校验**，而不该靠人工比对。`alfredc context` 实现时必须内置这个校验。

---

## F-008 · 文件改名会让历史事件的 `scope` 失效 🔴 已修正协议

**现象**：把协议从 `design/protocol-v0.1.md` 迁到 `spec/protocol.md` 后，**账本里 13 条历史事件的 `scope: ["design/protocol-v0.1.md"]` 全部指向一个不再存在的路径**。

而 `scope` 不是装饰——按 §9.4，它是 `--task` 过滤与**注入匹配**的依据。路径失效意味着：这些决策在将来按路径检索时**匹配不到**。

**这是机制 A 时代不会遇到的问题**：若账本是 Agent 实时写入的，路径在写入时总是当时的真实路径；但历史事件一旦落账，路径就冻结了，而代码会移动。

**两个处理方案**：

| 方案 | 评价 |
| --- | --- |
| 就地修正历史事件的 `scope` | ✅ 本次采用——因为账本**尚未首次提交** |
| 追加事件记录改名 + 编译器维护 rename 映射 | 长期方案，v1 不做 |

**修正**：确立「**不可变性的边界 = 账本的首次提交**」。提交前的启动阶段修正允许就地改；提交后只允许追加。本次迁移发生在首次提交之前，故就地修正。

**长期待办**：一旦账本已提交，路径失效就需要编译器支持 **rename 映射**（或 `scope` 改为语义化命名而非物理路径）。记为待办，v1 不实现——**因为单仓库内文件改名不频繁，而引入 rename 映射会让 `scope` 匹配复杂化**。

**产出**：`DEC-015`，协议 §3.2 补充说明。

> **这是本次自举最有价值的一条发现**：它揭示了"事件冻结了路径，而代码会移动"这一**机制 B 的固有张力**。机制 A 没有这个问题，机制 B 有——这正是选择 B 之后必须支付的成本之一。

---

## 汇总

| # | 摩擦 | 归宿 |
| --- | --- | --- |
| F-001 | 人工录入无 transcript 可引用 | 🔴 已修正协议（`DEC-013`） |
| F-002 | `constraints.md` 缺 Superseded 段 | 🟡 已修正协议（§6.2） |
| F-003 | 「全局作用域」无表示法 | 🟡 已修正协议（§5.1） |
| F-004 | 人工生成 `key` 别扭 | 🟢 设计取舍，保留观察 |
| F-005 | `CLAUDE.md` 符号链接跨平台隐患 | 🔴 待 `alfredc init` 处理 |
| F-006 | 无 CLI 时派生视图靠手工 | 🟢 自举的预期代价 |
| F-007 | §6.2 示例未覆盖全部权威分组 | 🟡 已修正协议（§6.2） |
| F-008 | 文件改名导致历史事件 `scope` 失效 | 🔴 已修正协议（`DEC-015`） |

**8 条摩擦中，5 条直接改进了协议。** 这个比例说明：协议在纸面上看起来完整，实际用起来仍有洞。

**最值得注意的一条**：F-001（人工录入无 transcript 可引用）如果没被自举发现，会一直潜伏到 `alfredc record` 实现时——那时改的是**代码 + 已迁移的数据**，成本高一个数量级。这就是"协议先行 + 手工自举"的价值。

---

## 待观察（下次自举时留意）

- `learnings/` 目录已建但为空——目前还没有"踩坑类"知识。它是被设计出来的，但尚未被真实需求驱动。**若长期为空，应考虑砍掉或并入 `decisions/`。**
- `tasks/` 目录同样未启用。协议 §4 定义了它，但手工阶段没有任务跟踪需求。
- `failed_attempt` 事件类型尚未被使用过。它的价值要等第一次"试错被否"才能验证。
